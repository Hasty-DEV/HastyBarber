import styled from "styled-components";

export const RegisterContainer = styled.main`
    button {
        border: none;
        outline: none;
        border: 1px solid #000;
    }

    .login-link {
        margin-left: 5px;
    }
`